# To be used like:
# docker build -t hrichardlee/meadowrun_test_env -f docker_images/TestEnvDockerfile docker_images

FROM python:3.10-slim-bullseye

WORKDIR /tmp/
RUN pip install requests pandas meadowrun
